package tupperdate.android.ui.theme

import tupperdate.android.BuildConfig
import tupperdate.android.R

/**
 * A placeholder image for profile pictures.
 */
const val PlaceholderProfileImage =
    "android.resource://${BuildConfig.APPLICATION_ID}/${R.drawable.placeholder_profile}"

/**
 * A placceholder for recipe pictures.
 */
const val PlaceholderRecipeImage =
    "android.resource://${BuildConfig.APPLICATION_ID}/${R.drawable.placeholder_recipe}"
